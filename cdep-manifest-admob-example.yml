example: |
  #include "firebase/app.h"
  #include "firebase/admob.h"
  
  void test() {
    ::firebase::admob::AdMob *admob = ::firebase::admob::AdMob::GetInstance(
      ::firebase::App::Create(::firebase::AppOptions(), NULL /* jni_env */ , NULL /* activity */ )
    );
  }
